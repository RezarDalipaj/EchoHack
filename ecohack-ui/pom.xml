<?xml version="1.0"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd" xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>de.dlh.lhind.springboot</groupId>
    <artifactId>ecohack-template</artifactId>
    <version>1.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>ecohack-ui</artifactId>
  <packaging>pom</packaging>

  <properties>
    <webapp.dir>.</webapp.dir>
  </properties>

  <build>
    <plugins>
      <plugin>
        <artifactId>maven-clean-plugin</artifactId>
        <version>2.5</version>
        <configuration>
          <filesets>
            <fileset>
              <directory>${webapp.dir}/dist</directory>
              <includes>
                <include>*</include>
                <include>**/*</include>
              </includes>
            </fileset>
            <fileset>
              <directory>${webapp.dir}/src/generated</directory>
              <includes>
                <include>*</include>
                <include>**/*</include>
              </includes>
            </fileset>
          </filesets>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>1.6.0</version>
        <executions>
          <execution>
            <id>install dependencies</id>
            <phase>generate-sources</phase>
            <configuration>
              <workingDirectory>${webapp.dir}/</workingDirectory>
              <executable>npm</executable>
              <arguments>
                <argument>install</argument>
                <argument>--save</argument>
                <argument>--legacy-peer-deps</argument>
              </arguments>
            </configuration>
            <goals>
              <goal>exec</goal>
            </goals>
          </execution>
          <execution>
            <id>build application</id>
            <phase>generate-sources</phase>
            <configuration>
              <workingDirectory>${webapp.dir}/</workingDirectory>
              <executable>npm</executable>
              <arguments>
                <argument>run</argument>
                <argument>build</argument>
              </arguments>
            </configuration>
            <goals>
              <goal>exec</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
